{
  "name": "base44ripper",
  "version": "0.0.1",
  "private": true,
  "description": "One-click escape from vibe-code platform lock-in. Upload a Base44 export, get a clean, runnable repo you own forever.",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @base44ripper/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20"
  }
}
